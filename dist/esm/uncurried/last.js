/**
 * @example
 * last([1, 2, 3]) // 3
 * last('foobar') // r
 */
export function last(it) {
    let value;
    for (value of it)
        ; // noop
    return value;
}
//# sourceMappingURL=last.js.map